<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%=bird.name%></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</head>
<body>
    <%- include('../partials/userview.ejs') %>
    <div class="px-4 pt-5 my-5 text-center border-bottom">
        <h1 class="display-4 fw-bold text-body-emphasis"><%=bird.name%></h1>
        <div class="col-lg-6 mx-auto">
            <div class="bd-example-snippet bd-code-snippet">
                <div class="bd-example m-0 border-0">
                      <ul class="list-group list-group-flush">
                        <li class="list-group-item">Continent(s): <%= bird.continent.join(", ") %></li>
                        <li class="list-group-item">Habitat(s): <%= bird.habitat.join(", ") %></li>
                        <li class="list-group-item">Diet: <%= bird.diet.join(", ") %></li>
                        <li class="list-group-item">Color: <%= bird.color%></li>
                        <li class="list-group-item">Wingspan: <%= bird.wingspan %>cm</li>
                        <% if (bird.isNocturnal === true) { %>
                            <li class="list-group-item">Nocturnal? Yes</li>
                        <% } else { %>
                            <li class="list-group-item">Nocturnal? No</li>
                        <% } %>
                      </ul>
                </div>
                <% if (user.isAdmin === true) {%>
                    <h3><a href="/birdapp/birds/<%= bird._id%>/edit">Edit <%= bird.name%></a></h3>  
                    <form action="/birdapp/birds/<%=bird._id%>?_method=DELETE" method="POST">
                        <button class="delete" type="submit">Delete <%=bird.name%> from index</button>
                    </form>
                    <% } %>
              </div>
          </div>
        </div>

</body>
</html>